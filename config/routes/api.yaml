api_example:
  resource: 'api/example.yaml'
  prefix: /api
  name_prefix: api_example_

api_agents:
    resource: 'api/agents.yaml'
    prefix: /api/agents
    name_prefix: api_agents_
    trailing_slash_on_root: false

api_space:
  resource: 'api/space.yaml'
  prefix: /api/spaces
  name_prefix: api_space_
  trailing_slash_on_root: false

