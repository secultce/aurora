{% extends "_layouts/blank.html.twig" %}

{% block title %} {{ 'control_panel'|trans }} - {{ 'view.opportunity.create'|trans }}{% endblock %}

{% block content %}
    <section class="d-flex">
        {% include "_components/side-bar.html.twig" %}

        <div class="management-content w-100 p-4">
            {% for message in app.flashes('error') %}
                <div class="alert alert-danger">
                    {{ message }}
                </div>
            {% endfor %}

            <div class="row justify-content-center">
                <div class="col-6">
                    <div class="card card-body shadow">
                        <h2>{{ 'view.opportunity.create'|trans }}</h2>

                        <hr>

                        <form action="{{ path('admin_opportunity_store') }}" method="post">
                            <input type="hidden" id="idOpportunity" name="id" value="{{ id }}">
                            <div class="mb-3">
                                <label for="typeOpportunity">Selecione o tipo da oportunidade</label><select name="typeOpportunity"
                                                                                            id=""></select></div>
                            <div class="mb-3">
                                <label for="nameOpportunity" class="mb-1">{{ 'name'|trans }}</label>
                                <input type="text" class="form-control" id="nameOpportunity" name="name" data-cy="name">
                            </div>
                            <div class="mb-3">
                                <label for="culturalLanguageOpportunity" class="mb-1">{{ 'cultural_language'|trans }}</label>
                                <select class="form-select" id="culturalLanguageOpportunity" name="extraFields[culturalLanguage]" data-cy="culturalLanguage">
                                    <option disabled selected></option>
                                    <option>Exposição</option>
                                    <option>Festival</option>
                                    <option>Musical</option>
                                </select>
                            </div>
                            <div class="mb-3">
                                <label for="areasOfExpertiseOpportunity" class="mb-1">{{ 'areas_of_expertise'|trans }}</label>
                                <select multiple class="form-select" id="areasOfExpertiseOpportunity" name="extraFields[areasOfExpertise][]" data-cy="areasOfExpertise">
                                    <option>Artesanato</option>
                                    <option>Cinema</option>
                                    <option>Dança</option>
                                    <option>Fotografia</option>
                                    <option>Outros</option>
                                </select>
                            </div>
                            <div class="mb-3">
                                <label for="initiativeCreatedBy" class="mb-1">{{ 'responsible_agent'|trans }}</label>
                                <select class="form-select" id="initiativeCreatedBy" name="createdBy" data-cy="createdBy">
                                    <option disabled selected></option>
                                    {% for item in opportunities %}
                                        <option value="{{ item.id }}">{{ item.name }}</option>
                                    {% endfor %}
                                </select>
                            </div>
                            <div class="mb-3">
                                <label for="coverImageOpportunity" class="mb-1">{{ 'cover_image'|trans }}</label>
                                <input type="file" class="form-control" id="coverImageOpportunity" accept="image/*" data-cy="coverImage">
                                <input type="hidden" id="base64CoverImageOpportunity" name="extraFields[coverImage]" value="">
                            </div>
                            <div class="mb-3">
                                <label for="shortDescriptionOpportunity" class="mb-1">{{ 'short_description'|trans }}</label>
                                <textarea class="form-control" rows="3" maxlength="400" id="shortDescriptionOpportunity" name="extraFields[shortDescription]" data-cy="shortDescription"></textarea>
                                <div class="clearfix">
                                    <small class="float-end">
                                        <span id="counterShortDescriptionOpportunity">0</span>/400
                                    </small>
                                </div>
                            </div>
                            <div class="mb-3">
                                <label for="longDescriptionOpportunity" class="mb-1">{{ 'long_description'|trans }}</label>
                                <textarea class="form-control" rows="3" maxlength="400" id="longDescriptionOpportunity" name="extraFields[longDescription]" data-cy="longDescription"></textarea>
                                <div class="clearfix">
                                    <small class="float-end">
                                        <span id="counterLongDescriptionOpportunity">0</span>/400
                                    </small>
                                </div>
                            </div>
                            <button type="submit" class="btn btn-primary me-3" data-cy="submit">{{ 'create_and_publish'|trans }}</button>
                            <button type="button" class="btn btn-secondary me-3">{{ 'create_in_draft'|trans }}</button>
                            <a href="{{ path('admin_initiative_list') }}" class="btn">{{ 'cancel'|trans }}</a>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}

{% block footer %}{% endblock %}

{% block extra_javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/initiative/create.js') }}"></script>
{% endblock %}
