<div class="item-card-home bg-white p-4 rounded shadow-sm mt-4">
    <div class="row align-items-center mt-3">
        <div class="col-md-3">
            <img src="{{ image_url|default(asset('img/default-image.png')) }}" alt="{{ image_alt|default('initiative.image'|trans) }}" class="rounded-circle img-fluid profile-image">
        </div>
        <div class="col-md-9 position-relative">
            <div class="d-flex align-items-center justify-content-end">
                <i class="material-icons text-success">{{ status_icon|default('check_circle') }}</i>
                <span>{{ status_text|default('opened_registrations'|trans) }}</span>
            </div>
        </div>
    </div>

    <div class="row align-items-start mt-3">
        <div class="d-flex justify-content-between align-items-start">
            <h4 class="fw-bold">
                {{ title|default('initiative.title'|trans) }}
            </h4>
        </div>
    </div>

    <p><span class="text-uppercase">{{ 'type'|trans }}:</span> <span class="text-green">{{ type }}</span></p>
    <p><span class="text-uppercase">{{ 'period'|trans }}:</span> <span class="text-info">{{ registration_period }}</span></p>

    <p>{{ description|default('description') }}</p>

    <h6 class="fw-bold">{{ 'tags'|trans }} ({{ tags_count|default(0) }}):</h6>
    <p class="fw-bold text-info">{{ tags }}</p>

    <div class="row mt-3">
        <div class="col-md position-relative">
            <a href="{{ link_url|default('#') }}" class="btn btn-primary d-flex align-items-center justify-content-center">
                <span class="fw-bold">{{ 'check'|trans }}</span>
                <i class="material-icons ms-3">arrow_forward</i>
            </a>
        </div>
    </div>
</div>