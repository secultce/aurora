{% extends "_layouts/blank.html.twig" %}

{% block title %}
    {{ 'initiatives'|trans }}
{% endblock %}

{% block content %}
    <section class="white-square">
        {% include '_components/breadcrumb.html.twig' with {
            items: [
                {text: 'home'|trans, path: path('admin_home_homepage')},
                {text: 'initiatives'|trans, path: path('admin_initiative_list')},
            ]
        } %}
        {% include '_components/title.html.twig' with {
            title: 'initiatives'|trans,
            action: {
                path: '#',
                text: 'view.initiative.create'|trans
            }
        } %}

        {% include "_components/entity-dashboard.html.twig" with { 'dashboard': dashboard} %}

        {% include 'admin/initiative/_partials/tabs/tabs.html.twig' %}
    </section>

    <section class="entity-wrapper">
        <div class="entity-container">
            {% include 'admin/initiative/_partials/tabs/content-tabs.html.twig' %}
        </div>
    </section>
{% endblock %}
