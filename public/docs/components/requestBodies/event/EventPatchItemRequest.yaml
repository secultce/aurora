type: object
properties:
  name:
    type: string
    maxLength: 100
    description: "Nome do evento"
  image:
    type: string
    format: base64url
    nullable: true
    description: "Imagem do perfil do evento codificada em Base64"
  agentGroup:
    type: string
    format: uuid
    description: "Grupo de agentes do evento"
  space:
    type: string
    format: uuid
    description: "Espaço do evento"
  initiative:
    type: string
    format: uuid
    description: "Iniciativa do evento"
  parent:
    type: string
    format: uuid
    description: "Evento pai"
  createdBy:
    type: string
    format: uuid
    description: "Identificador do agente criador do evento"
